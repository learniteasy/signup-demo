server:
  port: 8081
 
spring:
  application:
    name: signup
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8100
      
management:
  endpoints:
    web:
      exposure:
        include: -*
        